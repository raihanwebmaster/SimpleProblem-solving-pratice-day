#include<stdio.h>
int main(){
    int firstInteger;
    int secondInteger;
    int totalTwoInterger;
    printf("Input the first integer: ");
    scanf("%d", &firstInteger);
    printf("Input the second integer: ");
    scanf("%d", &secondInteger);
    printf("Product of the above two integers = %d ", firstInteger * secondInteger);
    return 0;
}
